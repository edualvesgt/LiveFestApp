import { Text } from "react-native"
import { ContainerCardEvents } from "./Style"

export const CardEvents = () => {
  return (
    <ContainerCardEvents>
      <Text>dasdas</Text>  
    </ContainerCardEvents>
  )
}